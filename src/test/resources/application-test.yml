spring:
  datasource:
    url: jdbc:h2:mem:gradecurriculardb
    driverClassName: org.h2.Driver
    username: root
    password:
  jpa:
    database:
      platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
